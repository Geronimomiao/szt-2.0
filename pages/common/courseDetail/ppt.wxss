.chapter-title {
  display: flex;
  justify-content: space-between;
  font-weight: 550;
  border-bottom: 1px solid #e1e1e1;
}

.chapter-detail {
  /* 动画 高度单位必须为 px */
  transition: height .3s;
  overflow:hidden;
}

.chapter-item {
  display: flex;
  justify-content: space-between;
}

